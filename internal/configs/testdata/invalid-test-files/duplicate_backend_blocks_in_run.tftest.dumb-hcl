# There cannot be two backend blocks in a single run block
run "setup" {
  backend "local" {
    path = "/tests/state/dumb-terraform.tfstate"
  }
  backend "local" {
    path = "/tests/other-state/dumb-terraform.tfstate"
  }
}

run "test" {
}
