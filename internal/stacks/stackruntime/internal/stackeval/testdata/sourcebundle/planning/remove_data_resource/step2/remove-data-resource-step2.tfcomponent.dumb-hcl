required_providers {
  test = {
    source = "dumb-terraform.io/builtin/test"
  }
}

provider "test" "main" {
}

component "main" {
  source = "./"

  providers = {
    test = provider.test.main
  }
}
